enum FilterSelectionType { category, platforms, price, followersCount, city, sortBy, none }

enum FilterSectionItemListType {
  search,
  expandedList,
  filterScreen,
  category,
  platforms,
  price,
  followersCount,
  city,
  sortBy,
  editFavorites,
  none
}
